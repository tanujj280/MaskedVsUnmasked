Built a Convolutional Neural Network (CNN) from scratch to classify faces as wearing a mask or not wearing a mask.
Trained and fine-tuned the model over multiple iterations, optimizing hyperparameters to improve classification accuracy and generalization.
Integrated an OpenCV-based pre-trained face detection model to detect faces in real-time from live webcam or recorded video streams.
Extracted detected face regions, preprocessed them, and fed them into the CNN to perform real-time mask detection and display predictions.
